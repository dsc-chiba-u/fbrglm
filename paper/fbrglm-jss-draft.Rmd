---
## ---- JSS submission metadata --------------------------------------
## This YAML is structured in JSS conventions. The active build target
## is `html_document` (the local environment has no LaTeX engine yet);
## see paper/README.md for the switch-over to rticles::jss_article.
## --------------------------------------------------------------------

title: "fbrglm: A Formula-Based Interface for Regularized Generalized Linear Models in R"
## For rticles::jss_article, replace the line above with the structured
## form below:
## title:
##   formatted: "fbrglm: A Formula-Based Interface for Regularized Generalized Linear Models in R"
##   plain:     "fbrglm: A Formula-Based Interface for Regularized GLMs in R"
##   short:     "fbrglm: Formula-Based Regularized GLMs"

author:
  - name: "Koki Tsuyuzaki"
    affiliation: "Chiba University"
    address: "TODO: department, postal address, country"
    email: "koki.tsuyuzaki@gmail.com"

date: "TODO: pin submission date"

abstract: >
  `glmnet` is the de-facto regularized generalized linear model engine in
  R, but its interface is built around a pre-constructed design matrix
  and a response vector. The gap between this style and base R's
  `glm()` is a recurring source of friction in applied work — factor
  handling, complete-case filtering, train/test design-matrix
  consistency, and a single λ-selection rule all land on the caller.
  This paper introduces fbrglm, a lightweight formula-based wrapper
  around `glmnet`. It accepts `formula` and `data`, stores
  training-time factor levels and contrasts on the fit object, rebuilds
  the prediction-time design matrix consistently, reports complete-case
  filtering explicitly, and exposes the underlying `glmnet` /
  `cv.glmnet` objects for downstream use. Current scope is
  `gaussian` / `binomial` / `poisson` families and `infer = "none"`;
  honest post-selection inference and additional families are deferred
  to later milestones. A small reproducible benchmark suite compares
  fbrglm to raw `glmnet`, `glmnetUtils`, and a `parsnip` /
  `workflows` pipeline.

keywords:
  - "regularized generalized linear models"
  - "glmnet"
  - "formula interface"
  - "R"
  - "prediction"
## For rticles::jss_article, JSS prefers the structured form:
## keywords:
##   formatted: [regularized generalized linear models, glmnet, formula interface, R, prediction]
##   plain:     [regularized generalized linear models, glmnet, formula interface, R, prediction]

output:
  html_document:
    toc: false
    number_sections: true
    self_contained: true
## When tinytex (or another LaTeX engine) is installed, swap the
## `output:` block above for:
##   output: rticles::jss_article
## and remove the html-specific `self_contained` key.

documentclass: jss
classoption: shortnames
bibliography: references.bib
link-citations: true
preamble: |
  \usepackage{amsmath}
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
    collapse = TRUE,
    comment  = "#>",
    fig.width  = 6,
    fig.height = 4
)
exp_dir <- "/home/koki/dev/fbrglm-experiments"
set.seed(20260101)
```

<style>
table { font-size: 85%; border-collapse: collapse; }
th, td { padding: 3px 8px; vertical-align: top; }
/* Keep section headings glued to the content that follows so example
   subsections (4.1, 4.2, ...) don't get separated from their code
   blocks under WeasyPrint pagination. */
h2, h3 { page-break-after: avoid; break-after: avoid; }
pre, table { page-break-inside: avoid; break-inside: avoid; }
</style>

# Introduction

Base R's `glm()` provides a familiar formula/data interface for
generalized linear models, automatic handling of factor predictors via
`model.matrix()`, complete-case filtering through `na.action`, and S3
methods (`print`, `summary`, `predict`, `coef`, `nobs`, `plot`) that
applied statisticians rely on. `glmnet` [@friedman2010glmnet;
@tay2023glmnetall] adds L1/L2 regularization to this family and is
extensively used in practice, but its API operates on a
caller-constructed design matrix `x` and response vector `y`. Several
ergonomic frictions follow:

- **Factor handling** is delegated to the caller. A common failure
  mode is that `model.matrix()` applied to a test data frame, in
  isolation, produces fewer columns than the matrix that was used at
  fit time when the test factor's levels are narrower than the
  training factor's, leading to a runtime error from
  `glmnet::predict()`.
- **Missingness** is not handled. Callers must filter rows themselves;
  there is no built-in "N rows dropped" report.
- **λ selection** is post-hoc on the `cv.glmnet` object
  (`$lambda.min`, `$lambda.1se`) or hand-supplied; the wrapper code
  needed to thread a single choice through fit, predict, and report is
  rewritten everywhere.

A goal of fbrglm is to package these conveniences into a wrapper that
*feels like `glm()`* and otherwise stays out of the way: no new
modelling abstraction, no new pipe, no opinion on inference.

fbrglm explicitly does **not** replace the `glmnet` computational
engine. `glmnet` and `cv.glmnet` are called verbatim under the hood,
and the resulting objects are stored on the `fbrglm` return value for
direct access. What fbrglm standardises is the surrounding
formula/data workflow and the prediction-time reconstruction of the
design matrix from training-time terms, contrasts, and factor levels.
A skilled raw-`glmnet` user can sidestep each individual friction
above by hand — by remembering to align `model.matrix()` columns, by
filtering complete cases before fitting, by threading a single λ
choice through their pipeline — but doing so reliably across every
project, every collaborator, and every test data slice is a workflow
problem rather than a modelling one.

The factor-level mismatch is the sharp example. When training and
test data are processed by independent `model.matrix()` calls, a
narrower-than-training factor in the test data silently produces a
narrower design matrix, and `glmnet::predict()` then errors at the
column count check. fbrglm makes this case work uniformly: training
terms, contrasts, factor levels, and column layout are stored on the
fit, the test design matrix is rebuilt against them, and any
training-time one-hot column that the test data does not exercise is
padded with zeros. Novel (unseen) factor levels remain hard errors;
fbrglm does not silently invent encodings.

<!-- TODO: cite practitioner papers / textbook treatments that motivate
the ergonomic gap between glm() and glmnet() once such citations are
collected. -->

## Design-matrix consistency as an API problem

The factor-level mismatch above is not a flaw in `glmnet`. The
optimizer correctly expects the caller to provide a design matrix
compatible with the one used at fit time, and `glmnet::predict()`'s
column-count check is the right defensive guard. The friction lives at
the *API boundary* between a formula/data workflow — where the design
matrix is constructed implicitly by `model.matrix()` from `data` — and
a matrix-based prediction interface that has no memory of how the
training matrix was constructed.

fbrglm treats the training design-matrix specification as part of the
fitted model object: `terms`, `xlevels`, `contrasts`, and the literal
training column names are stored alongside `coefficients` and
`lambda_value`, and they are reused at predict time to rebuild a
matrix that matches the fit. This is analogous to how `glm` objects
carry `terms` and `xlevels` so that `predict.glm(newdata = ...)` can
honour the original specification without re-deriving it from the
caller's hand-built matrix. The goal is reproducibility and reduced
caller burden, not a faster optimizer; fbrglm pays a small constant
overhead in exchange for prediction-time safety.

## Contributions

The contributions of this paper, and of the accompanying R package,
are:

- a `glm()`-like formula interface to `glmnet` for `gaussian`,
  `binomial`, and `poisson` models;
- prediction-time reconstruction and alignment of design matrices
  using training-time terms, contrasts, and factor levels, with
  zero-padding of any missing one-hot columns;
- explicit reporting of missing-row handling (`nobs_info`) and
  rank-deficiency diagnostics (`rank_info`);
- lightweight interoperability through accessors for the underlying
  `glmnet` and `cv.glmnet` objects (`as_glmnet()`, `as_cv_glmnet()`);
- a reproducible benchmark suite comparing fbrglm against raw
  `glmnet`, `glmnetUtils`, and a `parsnip` / `workflows` pipeline,
  released as a companion experiments repository.

# Related work

The R ecosystem already contains several packages that touch the same
ergonomic gap, with different scopes:

- **glmnetUtils** [@glmnetUtils] provides `glmnet(formula, data)` and
  `cv.glmnet(formula, data)` wrappers and a predict path that accepts
  `newdata`. It is the closest alternative to fbrglm in spirit. fbrglm
  takes a different scope: rather than focus solely on the formula
  surface, it makes the prediction-time design-matrix alignment
  explicit (via `xlevels` / `contrasts` / `x_colnames` carried on the
  fit and a single helper that pads missing one-hot columns), reports
  complete-case filtering through `nobs_info`, surfaces rank
  diagnostics through `rank_info`, and exposes the underlying engine
  objects through `as_glmnet()` / `as_cv_glmnet()`.
- **tidymodels** — specifically the **parsnip**, **workflows**,
  **recipes**, and **hardhat** packages [@parsnip; @workflows;
  @recipes; @hardhat] — offers a composable, framework-grade
  modelling pipeline in which `glmnet` is one of many engines and
  preprocessing is handled by hardhat / recipes. fbrglm's scope is
  intentionally narrower: it is a single-purpose, glm-like wrapper
  around `glmnet` for code paths that do not need a full workflow
  framework, recipes graph, or tuning vocabulary.
- **broom** [@broom] adds a tidy-output layer over many model
  objects, including `glmnet`. It is complementary to fbrglm rather
  than overlapping.
- **selectiveInference** [@selectiveInference; @tibshirani2016selective]
  is the canonical R toolkit for honest post-selection inference;
  fbrglm reserves `infer = "selective"` as a hook for delegating to it
  in a future release.

<!-- TODO: position fbrglm against parsnip::logistic_reg + glmnet engine
at greater length once a working manuscript draft exists. -->

## Scope of fbrglm

Within this landscape, `glmnetUtils` is the closest in spirit and
`tidymodels` is broader and more robust as a general workflow
framework. fbrglm deliberately targets the narrower middle ground:
the raw `glmnet` computational backend, a `glm`-like S3 object, and
explicit prediction-time design-matrix alignment. That narrower scope
is what allows the package to stay lightweight — there is no
preprocessing graph, no tuning vocabulary, and no engine abstraction
layer to maintain.

# Design and implementation

## `fbrglm()` interface

The user-facing entry point mirrors `stats::glm()`:

```r
fbrglm(
    formula, data,
    family = c("gaussian", "binomial", "poisson"),
    weights = NULL, offset = NULL,
    infer  = c("none", "split", "selective"),
    selection_frac = 0.2,
    alpha = 1,
    lambda = c("cv_min", "cv_1se", "fix"),
    lambda_value = NULL,
    x = NULL, y = NULL,
    ...
)
```

Arguments not native to `glmnet` (`infer`, `selection_frac`) are
recognised but only `infer = "none"` is implemented. `weights` and
`offset` are plumbed through to `glmnet()` / `cv.glmnet()`; the
underlying `lambda` rule decides which engine is called.

## Object structure

A fitted `fbrglm` object is a list of class
`c("fbrglm", "regularized_glm")` carrying, among other fields:

- `terms`, `xlevels`, `contrasts`, `x_colnames`, `x_train` — everything
  needed to rebuild a model matrix consistent with the training fit.
- `fit` (the underlying `glmnet` object) and `cv_fit` (`cv.glmnet`, or
  `NULL` for `lambda = "fix"`).
- `coefficients`, `nonzero`, `lambda_value`, `lambda_rule`.
- `nobs_info` (`n_total` / `n_dropped_missing` / `n_used`).
- `rank_info` (`rank` / `ncol` / `rank_deficient` / `pivot`).

## Prediction-time model matrix reconstruction

The training-time `terms`, `xlevels`, and `contrasts` are stored on
the fitted object at fit time. On `predict(object, newdata = ...)`,
`predict.fbrglm` passes `newdata` through
`model.frame(Terms, newdata, xlev = object$xlevels)` to re-level any
factor variables against the training-time levels, then calls
`model.matrix(Terms, mf, contrasts.arg = object$contrasts)` to encode
the rows under the same contrast scheme. An internal alignment helper
then reshapes the resulting matrix to `object$x_colnames`:

1. any training column missing from the test-derived matrix is padded
   with zeros (typically a one-hot column for a factor level that the
   test data did not exercise);
2. any extra column is dropped;
3. the remaining columns are reordered to match the training layout
   exactly.

Novel — i.e. unseen at fit time — factor levels are rejected at
`model.frame()` time and surface as hard errors. fbrglm deliberately
does not invent encodings for them: silently mapping a previously
unseen level to a zero row would change the meaning of the design
matrix without notice and is unsafe.

## λ selection

`lambda = "cv_min"` and `"cv_1se"` go through `glmnet::cv.glmnet()`;
`"fix"` skips CV and goes through
`glmnet::glmnet(lambda = lambda_value)`. The numeric λ actually used is
always recorded in `fit$lambda_value`.

## `nobs_info` and `rank_info`

Complete-case filtering happens via
`model.frame(..., na.action = na.omit)`, with the dropped count
surfaced both as a one-line message and as the `nobs_info` triple.
Rank deficiency is detected with `qr()` and recorded in
`rank_info$rank_deficient`; rank-deficient columns are **not** dropped
in the MVP (a warning is emitted instead).

## `as_glmnet()` / `as_cv_glmnet()`

These accessors expose the underlying `glmnet` / `cv.glmnet` objects so
that callers can fall back to any `glmnet`-specific tooling without
unwrapping the fbrglm object by hand.

# Usage examples

## Binomial fit

```{r}
library(fbrglm)
set.seed(1)
n <- 150
dat <- data.frame(
    y  = rbinom(n, 1, 0.5),
    x1 = rnorm(n), x2 = rnorm(n), x3 = rnorm(n)
)
fit <- fbrglm(y ~ x1 + x2 + x3, data = dat,
              family = "binomial", lambda = "cv_min")
fit$lambda_value
head(predict(fit, type = "response"))
```

## Factor predictor with narrowed test levels

The test data below contains only the factor levels `A` and `B`, even
though the model was fit with `A`, `B`, and `C`. fbrglm reuses the
training-time `xlevels` and `contrasts` stored on the fit object,
encodes the test rows through the same column layout, and pads any
training-time one-hot column the test data does not exercise with
zeros. Novel (unseen) factor levels remain hard errors at
`model.frame()` time.

```{r}
set.seed(2)
train <- data.frame(
    y  = rbinom(200, 1, 0.5),
    x1 = rnorm(200),
    g  = factor(sample(c("A", "B", "C"), 200, replace = TRUE),
                levels = c("A", "B", "C"))
)
fit_f <- fbrglm(y ~ x1 + g, data = train, family = "binomial",
                lambda = "fix", lambda_value = 0.05)
test <- data.frame(
    x1 = rnorm(5),
    g  = factor(rep(c("A", "B"), length.out = 5),
                levels = c("A", "B"))   # narrowed levels
)
predict(fit_f, newdata = test, type = "response")
```

## Offset

```{r}
fit_off <- fbrglm(y ~ x1 + g, data = train, family = "binomial",
                  offset = rep(0.2, nrow(train)),
                  lambda = "fix", lambda_value = 0.05)
head(predict(fit_off, type = "response"))                  # reuses offset
predict(fit_off,
        newdata   = train[1:3, ],
        newoffset = rep(0.2, 3),
        type = "response")
```

## Plot

```{r, eval = FALSE}
plot(fit)   # delegates to plot.cv.glmnet
```

# Benchmark experiments

Reproducible benchmarks live in the companion repository
[`fbrglm-experiments`](https://github.com/dsc-chiba-u/fbrglm-experiments).
The two summary tables below are generated by
`scripts/30_make_manuscript_tables.R` in that repository. This is a
**small benchmark** intended only as a first sanity check; the numbers
are **not intended to establish universal performance dominance** and
should be read **in the tested setting** only. A larger, more rigorous
suite (high-dimensional data, slow CV, inference variants, multiple
hardware) is planned. *(Hardware and R-version footnote to be added at
submission.)*

## What the benchmark does and does not show

The benchmark is scoped to two questions:

1. does each method preserve the prediction-time design-matrix
   contract when training and test factor levels disagree?
2. how much constant-factor overhead does each method add at fit and
   predict time on small data?

It does **not** attempt to compare predictive performance (no
hold-out loss is reported) and it does **not** sweep hyperparameters;
every method is run at the same fixed λ on identical inputs. Broader
claims of the form "which package is faster overall" or "which
package generalises better" would require a larger, hold-out-driven
study and are out of scope here.

## Prediction-failure benchmark

A binomial dataset is constructed so that the training factor `g` has
levels `A / B / C / D`, while the test factor's *levels* are narrowed
to `A / B` (the factor itself, not just the observed values). Each
method's behaviour at predict time is recorded.

```{r prediction-table, echo = FALSE}
pred_df <- utils::read.csv(
    file.path(exp_dir, "results", "summary",
              "manuscript_prediction_table.csv"),
    stringsAsFactors = FALSE
)
short_note <- c(
    "fbrglm"           = "auto alignment",
    "glmnet_raw_naive" = "column mismatch",
    "glmnet_raw_safe"  = "manual alignment",
    "glmnetUtils"      = "failed here",
    "parsnip_workflow" = "workflow preprocessing"
)
pred_show <- data.frame(
    method = pred_df$method,
    result = pred_df$prediction_under_narrowed_factor_levels,
    burden = pred_df$user_burden,
    ## unname() strips the named-vector names that data.frame() would
    ## otherwise lift into row.names, which kable would then render as
    ## a duplicate leading column.
    note   = unname(short_note[pred_df$method]),
    stringsAsFactors = FALSE
)
rownames(pred_show) <- NULL
knitr::kable(
    pred_show, format = "html", escape = TRUE,
    row.names = FALSE,
    col.names = c("method", "result", "burden", "note"),
    align = c("l", "l", "l", "l")
)
```

Both failure rows produce a `glmnet` runtime error of the form
*"The number of variables in newx must be N"*. fbrglm and the
`parsnip` / `workflows` path both succeed automatically; raw `glmnet`
succeeds when the caller manually aligns design-matrix columns.

## Runtime benchmark

Fit + predict wall-clock on a small grid of `(n, p_numeric, n_factor,
n_levels)` combinations, fixed λ = 0.05, median of 5 iterations from
`bench::mark()`. The compact summary below reports `glmnet_raw`'s
median in milliseconds and the per-scenario ratio of each other
method's median to it; values larger than 1× are slower than raw
`glmnet`.

```{r runtime-table, echo = FALSE}
rt_df <- utils::read.csv(
    file.path(exp_dir, "results", "summary",
              "manuscript_runtime_table.csv"),
    stringsAsFactors = FALSE
)
scn_order <- unique(rt_df$scenario)
pick <- function(scn, mthd, col) {
    rt_df[[col]][rt_df$scenario == scn & rt_df$method == mthd]
}
rt_wide <- do.call(rbind, lapply(scn_order, function(scn) {
    data.frame(
        scenario      = scn,
        raw_ms        = sprintf("%.1f", pick(scn, "glmnet_raw",       "median_ms")),
        fbrglm        = sprintf("%.2fx", pick(scn, "fbrglm",          "relative_to_raw_glmnet")),
        glmnetUtils   = sprintf("%.2fx", pick(scn, "glmnetUtils",     "relative_to_raw_glmnet")),
        parsnip       = sprintf("%.2fx", pick(scn, "parsnip_workflow","relative_to_raw_glmnet")),
        stringsAsFactors = FALSE
    )
}))
knitr::kable(
    rt_wide, format = "html", escape = TRUE,
    col.names = c("scenario", "glmnet_raw (ms)",
                  "fbrglm (× over raw)",
                  "glmnetUtils (× over raw)",
                  "parsnip_workflow (× over raw)"),
    align = c("l", "r", "r", "r", "r")
)
```

Method roles, repeated across scenarios: `glmnet_raw` is the fastest
matrix API; `fbrglm` is a lightweight formula interface; `glmnetUtils`
is a formula interface; `parsnip_workflow` is a full workflow
framework. Full per-iteration and per-method rows are available in the
companion experiments repository under
`results/raw/runtime_small_raw.csv` and
`results/summary/runtime_small.csv`.

![Runtime benchmark across four scenarios; bars are median of five iterations from `bench::mark()`. This is a small benchmark, not a universal performance claim.](../../fbrglm-experiments/results/figures/runtime_small.png)

The differences in this small benchmark line up with what each
method is actually doing: raw `glmnet` is the fastest because it
receives an already-built matrix and skips formula parsing; fbrglm
adds the formula-parsing pass plus the prediction-time alignment
helper; `glmnetUtils` is in a similar regime to fbrglm; the
`parsnip` / `workflows` path adds the broader workflow-framework
machinery on every call. The relevant takeaway is therefore that
fbrglm occupies a lightweight middle ground — *not* that fbrglm is
the fastest of the four. A "fastest" claim would require a larger
and more rigorous study.

# Limitations and future work

The MVP is deliberately narrow. The roadmap items below are tracked in
the package `TODO.md` and the manuscript will be expanded as they
land:

- `infer = "none"` only. `infer = "split"` (data splitting with
  `selection_frac`) and `infer = "selective"`
  [@selectiveInference; @tibshirani2016selective] are the next two
  inference modes.
- Families: `gaussian`, `binomial`, `poisson` are the core targets;
  fbrglm also accepts any GLM family object that `glmnet` itself
  accepts, so `stats::Gamma(link = "log")` and
  `MASS::negative.binomial(theta = ...)` (fixed θ) work as well.
  Native Cox (`family = "cox"` with `Surv(time, status) ~ ...`) and
  the multi-response cases (`multinomial`, `mgaussian`) are supported
  but marked experimental — basic fit / predict paths work, but the
  full breadth of usage (Cox strata and ties, multinomial grouped vs
  ungrouped) has not been exhaustively tested. Joint θ estimation in
  the spirit of `MASS::glm.nb()` is out of scope. A Cox-like survival
  fit is also expressible via the *piecewise exponential Poisson*
  formulation on long-format data; the *Families and model types*
  vignette walks through both routes side by side.
- The `x` / `y` direct-matrix entry point is reserved but not yet
  implemented.
- Larger-scale benchmarks (high-dimensional, slow CV, inference
  variants) are pending.
- CRAN / JSS submission readiness (vignette polish, additional unit
  tests, CI matrix expansion).

# Conclusion

fbrglm should be understood as a practical API layer over `glmnet`
rather than as a new modelling system. Its contribution is to make
formula-based regularized GLM workflows safer and more reproducible —
specifically, to make the prediction-time design matrix consistent
with the fit-time design matrix by default, and to surface the
diagnostic information (missing-row counts, rank status, underlying
`glmnet` objects) that calling code routinely needs. The empirical
claim from the small benchmark is intentionally modest: fbrglm
introduces a small constant overhead over raw `glmnet` in the tested
setting and is comparable to `glmnetUtils`, while remaining
substantially lighter than the `parsnip` / `workflows` path. Whether
richer inference modes — data splitting and selective inference — and
additional families can be added without losing this simplicity is
the central question of the next milestone.

# References
